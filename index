<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login | GF Grievance Portal</title>
  <style>
    body { font-family: Arial; text-align: center; background: #fff0f5; padding: 50px; }
    input { padding: 10px; margin: 10px; width: 300px; }
    button { padding: 10px 20px; background: #ff69b4; color: white; border: none; border-radius: 5px; }
  </style>
</head>
<body>
  <h1>💖 GF Grievance Portal Login</h1>
  <p>Enter your credentials to complain like royalty</p>

  <input id="loginUser" placeholder="Username"><br>
  <input id="loginPass" placeholder="Password" type="password"><br>
  <button onclick="login()">Login</button>

  <p><a href="register.html">New here? Register</a></p>
  <p id="error"></p>

  <script>
    function login() {
      const u = document.getElementById('loginUser').value;
      const p = document.getElementById('loginPass').value;
      const ru = localStorage.getItem("gfUser");
      const rp = localStorage.getItem("gfPass");

      if (u === ru && p === rp) {
        window.location.href = "portal.html";
      } else {
        document.getElementById("error").innerText = "❌ Wrong credentials. Are you even my GF?";
      }
    }
  </script>
</body>
</html>
